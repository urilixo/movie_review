<section>
    <ul>
        <% @users.each do |user| %>
            <li> <%= link_to user.name, user %>, created <%= time_ago_in_words(user.created_at)%> ago.</li>
        <% end %>
    </ul>    
</section>